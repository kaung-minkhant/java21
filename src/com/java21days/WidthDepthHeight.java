package com.java21days;

public class WidthDepthHeight { // if these are access within the same package public will not be necessary
  public int width = 0, depth = 0, height = 0; // if these are access within the same package public will not be necessary
  
  public void showAttributes() {
    System.out.println("Width: " + width + " Height: " + height + " Depth: " + depth);
  }
}
